<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Inscription - E-Sportify</title>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;500;700;900&family=Rajdhani:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary-purple: #9d4edd;
            --primary-blue: #4361ee;
            --primary-pink: #f72585;
            --glass-bg: rgba(10, 14, 35, 0.6);
            --glass-border: 1px solid rgba(255, 255, 255, 0.1);
            --glass-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.37);
        }

        body {
            margin: 0;
            padding: 0;
            font-family: 'Rajdhani', sans-serif;
            background: linear-gradient(rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.7)), url("{{ asset('images/gaming1.jpg') }}");
            background-size: cover;
            background-position: center;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
        }

        .login-container {
            background: var(--glass-bg);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            border: var(--glass-border);
            box-shadow: var(--glass-shadow);
            border-radius: 20px;
            padding: 40px;
            width: 100%;
            max-width: 450px;
            text-align: center;
            animation: slideIn 0.8s ease-out;
            position: relative;
        }

        @keyframes slideIn {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .logo {
            margin-bottom: 20px;
        }

        .logo img {
            height: 50px;
        }

        h2 {
            color: #fff;
            font-family: 'Orbitron', sans-serif;
            margin-bottom: 25px;
            letter-spacing: 2px;
            text-transform: uppercase;
        }

        .input-group {
            position: relative;
            margin-bottom: 20px;
            text-align: left;
        }

        .input-group input {
            width: 100%;
            padding: 12px 15px 12px 45px;
            background: rgba(0,0,0,0.3);
            border: 1px solid rgba(255,255,255,0.1);
            border-radius: 8px;
            color: #fff;
            font-family: 'Rajdhani', sans-serif;
            font-size: 16px;
            transition: all 0.3s ease;
            box-sizing: border-box;
        }

        .input-group input:focus {
            outline: none;
            border-color: var(--primary-blue);
            box-shadow: 0 0 10px rgba(67, 97, 238, 0.3);
            background: rgba(0,0,0,0.5);
        }

        .input-group i {
            position: absolute;
            left: 15px;
            top: 50%;
            transform: translateY(-50%);
            color: var(--primary-pink);
            font-size: 18px;
            pointer-events: none;
            transition: color 0.3s;
        }

        .input-group input:focus + i {
            color: var(--primary-blue);
        }

        .btn-login {
            background: linear-gradient(45deg, var(--primary-pink), var(--primary-purple));
            color: #fff;
            border: none;
            padding: 12px;
            width: 100%;
            border-radius: 8px;
            font-family: 'Orbitron', sans-serif;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: transform 0.3s, box-shadow 0.3s;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-top: 10px;
        }

        .btn-login:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(157, 78, 221, 0.4);
        }

        .links {
            margin-top: 20px;
            font-size: 14px;
        }

        .links a {
            color: rgba(255,255,255,0.7);
            text-decoration: none;
            transition: color 0.3s;
        }

        .links a:hover {
            color: var(--primary-cyan);
        }
    </style>
</head>
<body>

    <div class="login-container">
        <div class="logo">
            <img src="{{ asset('images/logo3.png') }}" alt="E-Sportify Logo">
            <h1 style="font-family: 'Orbitron', sans-serif; font-size: 24px; margin: 10px 0 0 0; background: linear-gradient(45deg, var(--primary-pink), #fff); -webkit-background-clip: text; -webkit-text-fill-color: transparent;">E-SPORTIFY</h1>
        </div>
        
        <h2>Créer un compte</h2>

        <form method="post">
            <div class="input-group">
                <input type="text" name="username" placeholder="Pseudo" required>
                <i class="fas fa-user"></i>
            </div>

            <div class="input-group">
                <input type="text" name="nom" placeholder="Nom" required>
                <i class="fas fa-id-card"></i>
            </div>

            <div class="input-group">
                <input type="email" name="email" placeholder="Email" required>
                <i class="fas fa-envelope"></i>
            </div>

            <div class="input-group">
                <input type="password" name="password" placeholder="Mot de passe" required>
                <i class="fas fa-lock"></i>
            </div>
            
            <div class="input-group">
                <input type="password" name="confirm_password" placeholder="Confirmer Mot de passe" required>
                <i class="fas fa-check-circle"></i>
            </div>

            <button type="submit" class="btn-login">Inscription Gratuite</button>
        </form>

        <div class="links">
            <a href="{{ path('app_login') }}">Déjà un compte ? Se Connecter</a>
        </div>
    </div>

</body>
</html>
