{% if announcements|length > 0 %}
    <div class="admin-tournament-table">
        <table data-table="announcements">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Tag</th>
                    <th>Titre</th>
                    <th>Lien</th>
                    <th>Création</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for announcement in announcements %}
                    <tr class="admin-tournament-row">
                        <td>#{{ announcement.id }}</td>
                        <td><span class="badge badge-info">{{ announcement.tag }}</span></td>
                        <td class="name">{{ announcement.title }}</td>
                        <td>{{ announcement.link|default('')|slice(0, 40) }}</td>
                        <td>{{ announcement.createdAt ? announcement.createdAt|date('d/m/Y H:i') : '' }}</td>
                        <td>
                            <div class="admin-tournament-row-actions">
                                <a class="admin-tournament-icon" href="{{ path('fil_admin_announcement_edit', {id: announcement.id}) }}" title="Voir">
                                    <i class="fas fa-eye"></i>
                                </a>
                                <a class="admin-tournament-icon cyan" href="{{ path('fil_admin_announcement_edit', {id: announcement.id}) }}" title="Modifier">
                                    <i class="fas fa-edit"></i>
                                </a>
                                <form method="post" action="{{ path('fil_admin_announcement_delete', {id: announcement.id}) }}" onsubmit="return confirm('Supprimer cette annonce ?');">
                                    <input type="hidden" name="_token" value="{{ csrf_token('delete_announcement_' ~ announcement.id) }}">
                                    <button class="admin-tournament-icon danger" type="submit" title="Supprimer">
                                        <i class="fas fa-trash"></i>
                                    </button>
                                </form>
                            </div>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
    <div class="admin-pagination-wrap">
        {{ knp_pagination_render(announcements, 'admin/_pagination.html.twig') }}
    </div>
{% else %}
    <div class="admin-tournament-empty">
        <div class="empty-ring"></div>
        <strong>Aucune annonce</strong>
        <p>Créez votre première annonce.</p>
    </div>
{% endif %}
