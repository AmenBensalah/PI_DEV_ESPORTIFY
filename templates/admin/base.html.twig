<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Admin Dashboard - E-Sportify{% endblock %}</title>
    
    <!-- Fonts & Icons -->
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;500;700;900&family=Rajdhani:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        :root {
            --bg-dark: #0f1015;
            --bg-panel: #1b1e2e;
            --primary-purple: #9d4edd;
            --primary-pink: #f72585;
            --primary-cyan: #4cc9f0;
            --primary-blue: #4361ee;
            --text-muted: #8d91a5;
            --radius-lg: 25px;
            --radius-md: 15px;
            --success-color: #00d084;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background-color: var(--bg-dark);
            color: #fff;
            font-family: 'Poppins', sans-serif;
            overflow-x: hidden;
        }

        /* Sidebar Styling */
        .admin-sidebar {
            width: 280px;
            height: 100vh;
            position: fixed;
            left: 0;
            top: 0;
            background: var(--bg-panel);
            display: flex;
            flex-direction: column;
            z-index: 1000;
            padding: 20px 0;
            border-right: 1px solid rgba(255,255,255,0.05);
            overflow-y: auto;
        }

        .admin-brand {
            padding: 0 30px;
            margin-bottom: 40px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .admin-brand img {
            height: 40px;
        }

        .admin-brand-text {
            font-size: 16px;
            font-weight: 800;
            background: linear-gradient(45deg, var(--primary-blue), #fff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .admin-brand-subtitle {
            font-size: 10px;
            color: var(--text-muted);
            letter-spacing: 2px;
        }

        .admin-nav {
            padding: 0;
            flex: 1;
        }

        .admin-nav-link {
            display: flex;
            align-items: center;
            padding: 14px 30px;
            color: var(--text-muted);
            text-decoration: none;
            transition: all 0.3s ease;
            font-weight: 500;
            font-size: 15px;
            border-left: 4px solid transparent;
            position: relative;
        }

        .admin-nav-link:hover, .admin-nav-link.active {
            color: #fff;
            background: linear-gradient(90deg, rgba(157, 78, 221, 0.1), transparent);
            border-left-color: var(--primary-purple);
        }

        .admin-nav-link.active::after {
            content: '';
            position: absolute;
            left: 0;
            top: 0;
            bottom: 0;
            width: 4px;
            background: var(--primary-purple);
            box-shadow: 0 0 10px var(--primary-purple);
        }

        .admin-nav-link i {
            width: 25px;
            margin-right: 15px;
            font-size: 18px;
        }

        .admin-sidebar-footer {
            padding: 20px 30px;
            border-top: 1px solid rgba(255,255,255,0.05);
            margin-top: auto;
        }

        .admin-sidebar-footer a {
            display: block;
            color: var(--text-muted);
            text-decoration: none;
            margin-bottom: 15px;
            font-size: 14px;
            transition: color 0.3s;
        }

        .admin-sidebar-footer a:hover {
            color: #fff;
        }

        .admin-sidebar-footer a.logout {
            color: var(--primary-pink);
            font-weight: 600;
        }

        .admin-content {
            margin-left: 280px;
            padding: 40px;
            min-height: 100vh;
        }

        .admin-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 40px;
        }

        .stat-card {
            background: var(--bg-panel);
            border-radius: var(--radius-lg);
            padding: 30px;
            transition: transform 0.3s ease;
            position: relative;
            overflow: hidden;
            border: none;
        }

        .card-gradient-purple {
            background: linear-gradient(135deg, #c77dff 0%, #7b2cbf 100%);
        }

        .card-gradient-blue {
            background: linear-gradient(135deg, #4cc9f0 0%, #4361ee 100%);
        }

        .admin-header h1 {
            font-family: 'Orbitron', sans-serif;
            font-size: 28px;
            margin: 0;
            background: linear-gradient(45deg, var(--primary-blue), var(--primary-pink));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .admin-user-info {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .admin-avatar {
            width: 45px;
            height: 45px;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--primary-blue), var(--primary-purple));
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
            font-size: 14px;
        }

        /* Table Styles */
        .admin-table-container {
            background: var(--bg-panel);
            border-radius: var(--radius-lg);
            padding: 30px;
            border: 1px solid rgba(255,255,255,0.05);
            overflow-x: auto;
        }

        .admin-table {
            width: 100%;
            border-collapse: collapse;
        }

        .admin-table th {
            background: transparent;
            color: var(--text-muted);
            font-weight: 600;
            border-bottom: 2px solid rgba(255,255,255,0.1);
            padding: 15px;
            text-align: left;
            font-size: 14px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        
        .admin-table td {
            padding: 18px 15px;
            border-bottom: 1px solid rgba(255,255,255,0.05);
            vertical-align: middle;
        }

        .admin-table tbody tr:hover {
            background: rgba(157, 78, 221, 0.05);
        }

        /* Badge Styles */
        .badge {
            border-radius: 30px;
            padding: 6px 14px;
            font-weight: 500;
            font-size: 13px;
            display: inline-block;
        }

        .badge-admin {
            background: linear-gradient(135deg, rgba(157, 78, 221, 0.3), rgba(247, 37, 133, 0.3));
            color: var(--primary-purple);
        }

        .badge-joueur {
            background: linear-gradient(135deg, rgba(67, 97, 238, 0.3), rgba(76, 201, 240, 0.3));
            color: var(--primary-cyan);
        }

        .badge-manager {
            background: linear-gradient(135deg, rgba(76, 201, 240, 0.3), rgba(67, 97, 238, 0.3));
            color: var(--primary-blue);
        }

        .badge-organisateur {
            background: linear-gradient(135deg, rgba(247, 37, 133, 0.3), rgba(157, 78, 221, 0.3));
            color: var(--primary-pink);
        }

        .status-active {
            color: var(--success-color);
        }

        /* Action Buttons */
        .btn-icon {
            background: transparent;
            border: 1px solid rgba(255,255,255,0.1);
            color: var(--text-muted);
            width: 35px;
            height: 35px;
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            margin-right: 8px;
        }

        .btn-icon:hover {
            background: rgba(157, 78, 221, 0.2);
            color: var(--primary-purple);
            border-color: var(--primary-purple);
        }

        .btn-gradient {
            background: linear-gradient(135deg, var(--primary-blue), var(--primary-pink));
            color: #fff;
            border: none;
            padding: 12px 25px;
            border-radius: 10px;
            cursor: pointer;
            font-weight: 600;
            transition: transform 0.3s, box-shadow 0.3s;
            font-size: 14px;
        }

        .btn-gradient:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(67, 97, 238, 0.3);
        }

        .user-avatar-small {
            display: flex;
            align-items: center;
            gap: 10px;
            font-weight: 600;
        }

        .avatar-circle {
            width: 35px;
            height: 35px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
            font-size: 12px;
            background: linear-gradient(135deg, var(--primary-blue), var(--primary-purple));
        }

        @media (max-width: 768px) {
            .admin-sidebar {
                width: 100%;
                height: auto;
                position: relative;
            }

            .admin-content {
                margin-left: 0;
            }
        }
    </style>
</head>
<body>

<aside class="admin-sidebar">
    <div class="admin-brand">
        <img src="{{ asset('images/logo3.png') }}" alt="Logo">
        <div>
            <div class="admin-brand-text">E-SPORTIFY</div>
            <div class="admin-brand-subtitle">ADMIN</div>
        </div>
    </div>

    <nav class="admin-nav">
        <a href="{{ path('admin_dashboard') }}" class="admin-nav-link {% if app.request.attributes.get('_route') == 'admin_dashboard' %}active{% endif %}">
            <i class="fas fa-chart-line"></i> Dashboard
        </a>
        <a href="{{ path('admin_users') }}" class="admin-nav-link {% if app.request.attributes.get('_route') == 'admin_users' %}active{% endif %}">
            <i class="fas fa-users"></i> Utilisateurs
        </a>
        <a href="{{ path('admin_equipes') }}" class="admin-nav-link {% if app.request.attributes.get('_route') == 'admin_equipes' %}active{% endif %}">
            <i class="fas fa-gamepad"></i> Équipes
        </a>
        <a href="{{ path('admin_manager_requests_index') }}" class="admin-nav-link {% if app.request.attributes.get('_route') == 'admin_manager_requests_index' %}active{% endif %}">
            <i class="fas fa-user-plus"></i> Demandes Manager
        </a>
        <div style="margin-top: 20px; margin-bottom: 5px; padding-left: 30px; font-size: 11px; text-transform: uppercase; color: var(--text-muted); letter-spacing: 1px;">
            Commandes
        </div>
        <a href="{{ path('admin_order_index') }}" class="admin-nav-link {% if app.request.attributes.get('_route') starts with 'admin_order' %}active{% endif %}">
            <i class="fas fa-receipt"></i> Commandes
        </a>
        <a href="{{ path('admin_payment_index') }}" class="admin-nav-link {% if app.request.attributes.get('_route') starts with 'admin_payment' %}active{% endif %}">
            <i class="fas fa-credit-card"></i> Paiements
        </a>
        <div style="margin-top: 20px; margin-bottom: 5px; padding-left: 30px; font-size: 11px; text-transform: uppercase; color: var(--text-muted); letter-spacing: 1px;">
            Boutique
        </div>
        <a href="{{ path('admin_boutique_produit_index') }}" class="admin-nav-link {% if app.request.attributes.get('_route') starts with 'admin_boutique_produit' %}active{% endif %}">
            <i class="fas fa-box-open"></i> Produits
        </a>
        <a href="{{ path('admin_boutique_categorie_index') }}" class="admin-nav-link {% if app.request.attributes.get('_route') starts with 'admin_boutique_categorie' %}active{% endif %}">
            <i class="fas fa-tags"></i> Catégories
        </a>
        <a href="{{ path('admin_tournois') }}" class="admin-nav-link {% if app.request.attributes.get('_route') == 'admin_tournois' %}active{% endif %}">
            <i class="fas fa-trophy"></i> Tournois
        </a>
        <a href="{{ path('admin_social') }}" class="admin-nav-link {% if app.request.attributes.get('_route') == 'admin_social' %}active{% endif %}">
            <i class="fas fa-newspaper"></i> Fil d'actualité
        </a>
        <a href="{{ path('admin_profile') }}" class="admin-nav-link {% if app.request.attributes.get('_route') == 'admin_profile' %}active{% endif %}">
            <i class="fas fa-user-circle"></i> Mon Profil
        </a>
    </nav>

    <div class="admin-sidebar-footer">
        <a href="{{ path('app_home') }}">
            <i class="fas fa-globe"></i> Voir le Site
        </a>
        <form method="post" action="{{ path('app_logout') }}" style="display: inline;">
            <button type="submit" style="background: none; border: none; color: var(--primary-pink); cursor: pointer; font-weight: 600; font-size: 14px; padding: 0; text-align: left; width: 100%;">
                <i class="fas fa-sign-out-alt"></i> Déconnexion
            </button>
        </form>
    </div>
</aside>

<main class="admin-content">
    <header class="admin-header">
        <h1>{% block page_title %}Dashboard{% endblock %}</h1>
        <div class="admin-user-info">
            <div class="admin-avatar">{{ app.user.nom|first|upper }}</div>
            <span style="font-weight: 600;">{{ app.user.nom }}</span>
        </div>
    </header>

    {% block body %}{% endblock %}
</main>

</body>
</html>
