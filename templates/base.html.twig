<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {% block stylesheets %}
            <style>
                @import url('https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600;700&family=Work+Sans:wght@400;500;600&display=swap');

                :root {
                    --bg-1: #0d1b2a;
                    --bg-2: #1b263b;
                    --card: #ffffff;
                    --ink: #0b1320;
                    --muted: #5b6b7b;
                    --accent: #2d6cdf;
                    --accent-2: #14b8a6;
                    --danger: #e11d48;
                    --ring: rgba(45, 108, 223, 0.25);
                }

                body {
                    margin: 0;
                    font-family: "Work Sans", "Segoe UI", sans-serif;
                    color: var(--ink);
                    background: radial-gradient(1200px 600px at 20% -10%, #2b6cb0 0%, rgba(43, 108, 176, 0.2) 45%, transparent 60%),
                                linear-gradient(180deg, #0e223a 0%, #0b1a2a 100%);
                    min-height: 100vh;
                }

                h1, h2, h3 {
                    font-family: "Playfair Display", Georgia, serif;
                    letter-spacing: 0.3px;
                }

                .container {
                    max-width: 1200px;
                    margin: 0 auto;
                    padding: 28px 20px 60px;
                    box-sizing: border-box;
                }

                .pro-card {
                    background: var(--card);
                    border-radius: 16px;
                    box-shadow: 0 12px 30px rgba(3, 10, 24, 0.2);
                    padding: 22px;
                    box-sizing: border-box;
                    max-width: 100%;
                }

                .pro-grid {
                    display: grid;
                    grid-template-columns: repeat(2, minmax(0, 1fr));
                    gap: 20px;
                }

                .pro-input {
                    width: 100%;
                    border: 1px solid #e6e9ef;
                    border-radius: 12px;
                    padding: 10px 12px;
                    font-size: 14px;
                    background: #f7f9fc;
                    transition: border-color 0.2s ease, box-shadow 0.2s ease, background 0.2s ease;
                }

                .pro-input:focus {
                    outline: none;
                    border-color: var(--accent);
                    box-shadow: 0 0 0 4px var(--ring);
                    background: #fff;
                }

                .pro-label {
                    font-size: 13px;
                    color: var(--muted);
                    margin-bottom: 6px;
                    display: inline-block;
                }

                .pro-btn {
                    border: none;
                    border-radius: 999px;
                    padding: 10px 18px;
                    font-weight: 600;
                    cursor: pointer;
                    display: inline-flex;
                    align-items: center;
                    gap: 8px;
                    transition: transform 0.2s ease, box-shadow 0.2s ease, background 0.2s ease;
                    text-decoration: none;
                    color: inherit;
                }

                .pro-btn-primary {
                    background: var(--accent);
                    color: #fff;
                    box-shadow: 0 10px 20px rgba(45, 108, 223, 0.35);
                }

                .pro-btn-primary:hover {
                    transform: translateY(-1px);
                }

                .pro-btn:hover {
                    transform: translateY(-1px);
                }

                .pro-btn:active {
                    transform: translateY(0);
                }

                .pro-btn:focus-visible {
                    outline: none;
                    box-shadow: 0 0 0 4px var(--ring);
                }

                .pro-btn-danger {
                    background: var(--danger);
                    color: #fff;
                }

                .pro-btn-secondary {
                    background: #e2e8f0;
                    color: #0f172a;
                }

                .pro-link {
                    color: var(--accent);
                    text-decoration: none;
                    font-weight: 600;
                }

                .pro-link:hover {
                    text-decoration: underline;
                }

                .btn {
                    border: none;
                    border-radius: 999px;
                    padding: 10px 18px;
                    font-weight: 600;
                    cursor: pointer;
                    display: inline-flex;
                    align-items: center;
                    gap: 8px;
                    transition: transform 0.2s ease, box-shadow 0.2s ease, background 0.2s ease, color 0.2s ease;
                    text-decoration: none;
                }

                .btn-primary {
                    background: var(--accent);
                    color: #fff;
                    box-shadow: 0 10px 20px rgba(45, 108, 223, 0.35);
                }

                .btn-secondary {
                    background: #e2e8f0;
                    color: #0f172a;
                }

                .btn-success {
                    background: var(--accent-2);
                    color: #fff;
                    box-shadow: 0 10px 20px rgba(20, 184, 166, 0.35);
                }

                .btn-danger {
                    background: var(--danger);
                    color: #fff;
                }

                .btn:hover {
                    transform: translateY(-1px);
                }

                .alert {
                    border-radius: 14px;
                    padding: 12px 16px;
                    box-shadow: 0 10px 24px rgba(15, 23, 42, 0.12);
                }

                .form-control, input[type="text"], input[type="number"], input[type="email"], input[type="password"], select, textarea {
                    width: 100%;
                    border: 1px solid #e6e9ef;
                    border-radius: 12px;
                    padding: 10px 12px;
                    font-size: 14px;
                    background: #f7f9fc;
                    transition: border-color 0.2s ease, box-shadow 0.2s ease, background 0.2s ease;
                    box-sizing: border-box;
                }

                .form-control:focus,
                input[type="text"]:focus,
                input[type="number"]:focus,
                input[type="email"]:focus,
                input[type="password"]:focus,
                select:focus,
                textarea:focus {
                    outline: none;
                    border-color: var(--accent);
                    box-shadow: 0 0 0 4px var(--ring);
                    background: #fff;
                }

                label {
                    font-size: 13px;
                    color: var(--muted);
                    margin-bottom: 6px;
                    display: inline-block;
                }

                .table {
                    width: 100%;
                    border-collapse: collapse;
                    background: #ffffff;
                    border-radius: 14px;
                    overflow: hidden;
                    box-shadow: 0 12px 28px rgba(3, 10, 24, 0.12);
                }

                .table th, .table td {
                    text-align: left;
                    padding: 12px 14px;
                    border-bottom: 1px solid #eef1f6;
                    font-size: 14px;
                }

                .table th {
                    background: #f1f5f9;
                    color: #0f172a;
                    font-weight: 600;
                }

                .table tr:last-child td {
                    border-bottom: none;
                }

                .mb-3 {
                    margin-bottom: 14px;
                }

                .cart-page h1 {
                    color: #f8fafc;
                    margin-bottom: 18px;
                }

                .cart-shell {
                    display: grid;
                    grid-template-columns: 1.1fr 1fr;
                    gap: 22px;
                }

                .cart-shell.single {
                    grid-template-columns: 1fr;
                    max-width: 520px;
                    margin: 0 auto;
                }

                .cart-page .pro-card {
                    width: 100%;
                }

                .form-rows {
                    display: grid;
                    gap: 10px;
                    padding: 0;
                }

                .form-row {
                    padding: 0 12px;
                }

                .form-row {
                    display: grid;
                    grid-template-columns: 1fr;
                    gap: 8px;
                }

                .form-row .pro-label {
                    margin: 0;
                    font-size: 10px;
                    color: #6b7280;
                    text-transform: none;
                    letter-spacing: 0.2px;
                }

                .form-row .pro-input {
                    height: 32px;
                    border-radius: 8px;
                    background: #fff;
                    border: 1px solid #e5e7eb;
                }

                .pro-input::placeholder {
                    color: #9aa3af;
                    font-size: 12px;
                }

                .form-section-title {
                    font-family: "Work Sans", "Segoe UI", sans-serif;
                    font-size: 12px;
                    font-weight: 600;
                    letter-spacing: 0.6px;
                    text-transform: uppercase;
                    color: #6b7280;
                    margin-bottom: 6px;
                }

                @media (max-width: 600px) {
                    .container {
                        padding: 20px 14px 40px;
                    }

                    .form-row {
                        grid-template-columns: 1fr;
                        align-items: start;
                    }
                }

                .cart-list {
                    list-style: none;
                    padding: 0;
                    margin: 0;
                }

                .cart-item {
                    display: flex;
                    justify-content: space-between;
                    align-items: center;
                    padding: 12px 0;
                    border-bottom: 1px solid #eef1f6;
                }

                .cart-item:last-child {
                    border-bottom: none;
                }

                .cart-total {
                    margin-top: 16px;
                    font-weight: 600;
                    color: var(--ink);
                }

                .cart-actions {
                    display: flex;
                    gap: 10px;
                    flex-wrap: wrap;
                    margin-top: 16px;
                    align-items: center;
                }

                .cart-actions.stack {
                    flex-direction: column;
                    align-items: flex-start;
                }

                .cart-actions.space {
                    justify-content: space-between;
                    width: 100%;
                }

                .cart-actions .pro-link {
                    font-weight: 600;
                    font-size: 14px;
                }

                .pro-btn {
                    min-width: 140px;
                    justify-content: center;
                }

                .info-list {
                    display: grid;
                    gap: 10px;
                    padding: 0 12px;
                }

                .info-item {
                    background: #f9fafb;
                    border: 1px solid #e5e7eb;
                    border-radius: 10px;
                    padding: 10px 12px;
                    color: #111827;
                    font-size: 14px;
                }

                @media (max-width: 900px) {
                    .pro-grid {
                        grid-template-columns: 1fr;
                    }

                    .cart-shell {
                        grid-template-columns: 1fr;
                    }
                }
            </style>
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
            <script>
                const autoDismissAlerts = () => {
                    const alerts = document.querySelectorAll('.alert');
                    if (!alerts.length) return;
                    setTimeout(() => {
                        alerts.forEach((alert) => {
                            alert.style.transition = 'opacity 0.4s ease, transform 0.4s ease';
                            alert.style.opacity = '0';
                            alert.style.transform = 'translateY(-6px)';
                            setTimeout(() => alert.remove(), 450);
                        });
                    }, 2000);
                };

                const initCartSearch = () => {
                    const input = document.getElementById('cart_search');
                    if (!input) return;
                    const items = Array.from(document.querySelectorAll('.cart-item[data-search]'));
                    input.addEventListener('input', () => {
                        const q = input.value.trim().toLowerCase();
                        items.forEach((item) => {
                            const hay = (item.getAttribute('data-search') || '').toLowerCase();
                            item.style.display = hay.includes(q) ? '' : 'none';
                        });
                    });
                };

                document.addEventListener('DOMContentLoaded', () => {
                    autoDismissAlerts();
                    initCartSearch();
                });
                document.addEventListener('turbo:load', () => {
                    autoDismissAlerts();
                    initCartSearch();
                });
            </script>
        {% endblock %}
    </head>
    <body>
        <div class="container">
            {% for type, messages in app.flashes %}
                <div class="alert alert-{{ type }}" style="padding: 10px; margin: 10px 0; border-radius: 5px; {% if type == 'success' %}background-color: #d4edda; color: #155724; border: 1px solid #c3e6cb;{% elseif type == 'error' %}background-color: #f8d7da; color: #721c24; border: 1px solid #f5c6cb;{% else %}background-color: #d1ecf1; color: #0c5460; border: 1px solid #bee5eb;{% endif %}">
                    {% for message in messages %}
                        <p style="margin: 5px 0;">{{ message }}</p>
                    {% endfor %}
                </div>
            {% endfor %}
            
            {% block body %}{% endblock %}
        </div>
    </body>
</html>
